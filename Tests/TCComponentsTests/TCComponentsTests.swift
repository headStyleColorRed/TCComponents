    import XCTest
    @testable import TCComponents

    final class TCComponentsTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(TCComponents().text, "Hello, World!")
        }
    }
